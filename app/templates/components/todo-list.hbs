{{title}}
<br>
{{#each todos as |todo idx|}}
  {{detail-modal title=todo.title desc=todo.desc delete=attrs.delete listIdx=attrs.listIdx todoIdx=idx}}
{{/each}}


{{#if addTodo}}
  {{input enter="saveTodo" placeholder="Type something..." value=newTodoTitle}}
  <br>
  <button {{action "saveTodo"}}>Save</button>
  <button {{action "cancel"}}>Cancel</button>
{{else}}
  <button {{action "addTodo"}}>Add an item...</button>
{{/if}}


{{yield}}
