{{title}}
<br>
{{#each todos as |todo|}}
  <div>{{todo}}</div>
{{/each}}

{{#if addTodo}}
  {{input placeholder="Enter your todo" value=newTodo}}
  {{input type="hidden" value=listIndex}}

  <br>
  <button {{action "saveTodo"}}>Save</button>
  <button {{action "cancel"}}>Cancel</button>
{{else}}
  <button {{action "addTodo"}}>Add todo</button>
{{/if}}


{{yield}}
